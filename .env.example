# Supabase Configuration
# Get these values from your Supabase project settings (Settings → API)

# For development, copy this file to .env.development
# For production, copy this file to .env.production

VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
VITE_SUPABASE_ANON_KEY="your-public-anon-key"

# Service role key - ONLY for admin operations
# ⚠️ WARNING: This key has full access. Never commit it or expose it in production!
# In production, move admin operations to a secure backend.
VITE_SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
